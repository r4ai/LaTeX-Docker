name: Build and push docker image
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Login to Decker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
    - name: Build and push latex image
      uses: docker/build-push-action@v3
      with:
        context: ./
        file: ./docker/latex/Dockerfile
        builder: ${{ steps.buildx.outputs.name }}
        push: true
        tags: ${{ secrets.DOCKER_HUB_USERNAME }}/latex:latest
        chache-from: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/latex:buildcache
        chache-to: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/latex:buildcache,mode=max
    - name: Build and push pandoc image
      uses: docker/build-push-action@v3
      with:
        context: ./
        file: ./docker/pandoc/Dockerfile
        builder: ${{ steps.buildx.outputs.name }}
        push: true
        tags: ${{ secrets.DOCKER_HUB_USERNAME }}/pandoc:latest
        chache-from: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/pandoc:buildcache
        chache-to: type=registry,ref=${{ secrets.DOCKER_HUB_USERNAME }}/pandoc:buildcache,mode=max
